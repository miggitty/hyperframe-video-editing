# Copy this file to `.env` (in the workspace root) and fill in your own values.
# `.env` is gitignored — never commit your real keys.

# kie.ai API key — used by .claude/skills/how-to-edit-videos/scripts/generate_images.py
# to generate cutaway infographics with the gpt-image-2 model.
# Get one at: https://kie.ai/
KIE_API_KEY=your-kie-ai-key-here
